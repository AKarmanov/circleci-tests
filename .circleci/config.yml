version: 2.1
jobs:
  test-job:
    docker:
      - image: ubuntu:14.04

    working_directory: ~/repo

    steps:
      - checkout
      - run: echo 'Hello there!!!!'
    
workflows:
  version: 2.1
  "My workflow":
    jobs:
      - test-job
      
